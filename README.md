First Of All, we have to setup the Environment .where,
Language: Python 3.10.9
check: python --version
Framework: Selenium WebDriver  
Browser: Google Chrome  
Driver: ChromeDriver -Version 149.0.7827.54

After That, we have to install selenium web automation tool.
Install Selenium: to check web automation automatically by CMD,
pip install selenium
also this one::  pip install selenium webdriver-manager

project folder: qa-signup-automation
put the script insude: signup_test.py
then, we used the CMD with the path:  cd  C:\Users\KIIT0001\Desktop\qa-signup-automation

How to Run
python signup_test.py

What it does?
- Opens website
- Fills signup form
- Submits form
- Shows result in terminal
